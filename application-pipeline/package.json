{
  "name": "application-pipeline",
  "private": true,
  "type": "module",
  "scripts": {
    "types": "wrangler types",
    "pretest": "npm run types",
    "test": "vitest run",
    "predeploy": "npm run types",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "typescript": "^5.7.0",
    "vitest": "4.1.11",
    "wrangler": "^4.131.2"
  }
}
